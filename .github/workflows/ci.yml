name: CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  # =========================
  # MOBILE
  # =========================
  mobile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze
        continue-on-error: true
        
      - name: Run tests with coverage
        run: flutter test --coverage

      - name: Check coverage file
        run: ls -la coverage

      - name: Upload mobile coverage
        uses: actions/upload-artifact@v4
        with:
          name: mobile-coverage
          path: mobile/coverage/lcov.info
          if-no-files-found: error


  # =========================
  # FRONTEND
  # =========================
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: implementations/frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
<<<<<<< HEAD
          node-version: "20"
          
=======
          node-version: 20

>>>>>>> ea32986 (update ci.yml config)
      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run tests with coverage
        run: npm test -- --coverage --ci --watchAll=false
<<<<<<< HEAD
=======

      - name: Debug coverage (frontend)
        run: |
          ls -R coverage || true
          head -20 coverage/lcov.info || true
>>>>>>> ea32986 (update ci.yml config)

      - name: Upload frontend coverage
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: implementations/frontend/coverage/lcov.info
          if-no-files-found: error


  # =========================
  # BACKEND
  # =========================
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: implementations/backend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
<<<<<<< HEAD
          node-version: "20"
=======
          node-version: 20
>>>>>>> ea32986 (update ci.yml config)

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Debug coverage (backend)
        run: |
          ls -R coverage || true
          head -20 coverage/lcov.info || true

      - name: Upload backend coverage
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: implementations/backend/coverage/lcov.info
          if-no-files-found: error


  # =========================
  # SONAR
  # =========================
  sonar:
    runs-on: ubuntu-latest
    needs: [mobile, frontend, backend]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

<<<<<<< HEAD
      # =====================
      # DOWNLOAD COVERAGE
      # =====================
=======
>>>>>>> ea32986 (update ci.yml config)
      - name: Download frontend coverage
        uses: actions/download-artifact@v4
        with:
          name: frontend-coverage
          path: implementations/frontend/coverage

      - name: Download backend coverage
        uses: actions/download-artifact@v4
        with:
          name: backend-coverage
          path: implementations/backend/coverage

      - name: Download mobile coverage
        uses: actions/download-artifact@v4
        with:
          name: mobile-coverage
          path: mobile/coverage

<<<<<<< HEAD
      - name: Verify coverage files
        run: |
          ls -R implementations/frontend/coverage || true
          ls -R implementations/backend/coverage || true
          ls -R mobile/coverage || true

      # =====================
      # SONAR SCAN
      # =====================
=======
      - name: Debug all coverage (before sonar)
        run: |
          echo "=== FRONTEND ==="
          ls -R implementations/frontend/coverage || true
          echo "=== BACKEND ==="
          ls -R implementations/backend/coverage || true
          echo "=== MOBILE ==="
          ls -R mobile/coverage || true

>>>>>>> ea32986 (update ci.yml config)
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: |
            -Dsonar.projectKey=outoft3n_2025-ITCS383-Chongyai3
            -Dsonar.organization=outoft3n
            -Dsonar.projectVersion=${{ github.run_number }}

            -Dsonar.sources=implementations/backend/src,implementations/frontend/src,mobile/lib

            -Dsonar.tests=implementations/backend/src,implementations/frontend,mobile/test
            -Dsonar.test.inclusions=**/*.test.*,**/*.spec.*

            -Dsonar.javascript.lcov.reportPaths=implementations/backend/coverage/lcov.info,implementations/frontend/coverage/lcov.info
            -Dsonar.dart.lcov.reportPaths=mobile/coverage/lcov.info

            -Dsonar.coverage.exclusions=**/*.tsx,**/*.jsx
            -Dsonar.exclusions=**/node_modules/**,**/*.md,designs/**,images/**

            -Dsonar.c.file.suffixes=-
            -Dsonar.cpp.file.suffixes=-
            -Dsonar.objc.file.suffixes=-